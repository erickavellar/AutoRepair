@model AutoRepair.Models.ScheduleHeldViewModel
@{
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
    ViewData["Title"] = "ScheduleHeld";
}

<h2 class="col-md-4 offset-md-4" style="color:cornflowerblue; font-style:italic; margin-top:12%">Schedule Date Held</h2>
<br />
<br />
<div class="row">
    <div class="col-md-4 offset-md-4">
        <form asp-action="ScheduleHeld">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />

            <div class="form-group">
                <label asp-for="ScheduleDateHeld" class="control-label"></label>
                <input asp-for="ScheduleDateHeld" class="form-control" style="color:black" />
                <span asp-validation-for="ScheduleDateHeld" class="text-danger"></span>
            </div>

            

            <div class="form-group">
                <button type="submit" value="Save" class="btn btn-primary"><i class="fa fa-save "></i> Save</button>
                <a asp-action="Index" class="btn btn-success"><i class="fa fa-chevron-left"></i> Back to List</a>
            </div>
        </form>
    </div>
</div>





        @*<script type="text/javascript">
            $('.datepicker').datepicker();

    $(document).ready(function () {
                var userLang = navigator.language || navigator.userLanguage;
                console.log(userLang);
                var options = $.extend({}, // empty object
                    $.datepicker.regional[pt - Pt], {
                    dateFormat: "dd/mm/yyyy"
                } // your custom options
                );
                $(".date").datepicker(options);
    });



        </script>*@
    }
}

